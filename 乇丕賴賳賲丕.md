# راهنمای اپلیکیشن مکتب نورانیه مند میتاب

## این اپلیکیشن چیست؟
یک اپلیکیشن وب (PWA) جامع برای مدیریت مکتب قرآنی است که کاملاً **آفلاین** کار می‌کند و روی گوشی موبایل **نصب** می‌شود — دقیقاً مثل یک اپلیکیشن واقعی، بدون نیاز به گوگل‌پلی یا اینترنت.

## بخش‌های اپلیکیشن
- 🧑‍🎓 **طلاب**: ثبت‌نام، ویرایش و حذف اطلاعات طلاب
- ✅ **حضور و غیاب**: ثبت روزانه حضور/غیاب/رخصت به تفکیک کلاس
- 📊 **نمرات و کارنامه**: ثبت نمرات هر مضمون، محاسبه خودکار معدل و رتبه
- 📖 **حفظ قرآن**: پیگیری میزان حفظ (جزء/سوره) هر طلبه با نوار پیشرفت
- 🏫 **کلاس‌ها و اساتید**: ایجاد کلاس، تعیین استاد مسئول

تمام اطلاعات روی همان گوشی/مرورگری که اپ را باز می‌کنید ذخیره می‌شود.

## روش نصب (پیشنهادی — بهترین حالت)
برای اینکه دکمهٔ «نصب» واقعی روی گوشی ظاهر شود، باید فایل‌ها را روی یک آدرس اینترنتی (هاست) قرار دهید. ساده‌ترین و رایگان‌ترین راه:

1. یک حساب رایگان در **GitHub** بسازید (github.com)
2. یک مخزن (Repository) جدید بسازید، مثلاً به نام `maktab-app`
3. تمام فایل‌های این پوشه (`index.html`، `manifest.json`، `sw.js`، پوشهٔ `icons`) را در آن آپلود کنید
4. از تنظیمات مخزن، بخش **Pages** را فعال کنید (Settings → Pages → Branch: main)
5. بعد از چند دقیقه، یک آدرس مثل `https://username.github.io/maktab-app` برایتان ساخته می‌شود
6. آن آدرس را با گوشی باز کنید → در مرورگر کروم گزینهٔ «Add to Home Screen» یا «نصب اپلیکیشن» را بزنید

اگر با این مرحله مشکل دارید یا نمی‌خواهید گیت‌هاب استفاده کنید، هر سرویس هاستینگ رایگان دیگر (مثل Netlify یا Vercel) هم کار می‌کند — کافی است فایل‌ها را در آنجا آپلود کنید.

## روش سریع (بدون هاست، برای تست فوری)
فایل `index.html` را مستقیماً با مرورگر گوشی باز کنید. اپلیکیشن کار می‌کند و اطلاعات ذخیره می‌شود، اما دکمهٔ «نصب» رسمی PWA در این حالت معمولاً نمایش داده نمی‌شود (چون گوشی‌ها این ویژگی را فقط برای صفحات آنلاین با آدرس واقعی فعال می‌کنند).

## قابل ویرایش است
تمام کدها ساده و باز هستند (HTML/CSS/JavaScript، بدون نیاز به نصب برنامه‌نویسی خاصی). اگر خواستید رنگ‌ها، نام مکتب، مضامین درسی یا هر بخش دیگر را تغییر دهید، کافی است بگویید تا برایتان اصلاح کنم.

## نکته دربارهٔ پشتیبان‌گیری
چون اطلاعات فقط روی همان دستگاه ذخیره می‌شود، بهتر است هر چند وقت یک‌بار (مثلاً هر ماه) از من بخواهید یک نسخهٔ خروجی Excel از اطلاعات طلاب/نمرات تهیه کنم تا در صورت خرابی گوشی، داده‌ای از دست نرود.
